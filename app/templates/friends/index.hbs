<table class="primary">
  <thead>
    <tr>
      <th>Name</th>
      <th>Articles</th>
      <th></th>
    </tr>
  </thead>
    <hr />
  <tbody>
    {{#each friend in model}}
      <tr>
        <td>{{link-to friend.fullName "articles" friend}}</td>
        <td>{{link-to “Lend articles” “articles.new” friend}}</td>
        <td>{{friend.totalArticles}}</td>
        <td><a href="#" {{action "delete" friend}}>delete</a></td>
      </tr>
    {{/each}}
  </tbody>
</table>
